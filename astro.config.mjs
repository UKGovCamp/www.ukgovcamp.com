// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://new.ukgovcamp.com",
  image: {
    responsiveStyles: true,
    layout: "full-width",
  },
});
